import axios from 'axios';
import { URL } from './../utils/generalVariables';

export const getUserHandler = (token, email) => {

}

export const changePasswordHandler = (token, currentPassword, newPassword) => {

}

export const deleteUserHandler = (token) => {

}